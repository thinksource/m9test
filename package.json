{
  "name": "m9test",
  "version": "0.0.1",
  "author": "Sheng",
  "license": "MIT",
  "description": "nothing",
  "scripts": {
    "start": "node ./bin/www",
	  "dev":"nodemon ./bin/www",
    "test": "mocha && mocha test --require blanket --reporter html-cov > coverage.html"
  },
  "main": "./bin/www",
  "dependencies": {
    "express": "^4.14.0",
    "body-parser": "^1.15.2"
  },
  "devDependencies": {
    "blanket": "latest",
    "chai": "latest",
    "mocha": "^3.2.0",
    "nodemon": "^1.11.0",
    "unit.js": "^2.0.0"
  },
  "config": {
    "blanket": {
      "pattern": [
        "app.js"
      ],
      "data-cover-never": "node_modules"
    }
  }
}
